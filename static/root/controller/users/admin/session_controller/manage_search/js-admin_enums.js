const COMMANDS = {
  S_SUBMIT_SEARCH : 2,
};

const KEY = {
  S_COMMAND : 'p_command',
  S_SUBMIT_SEARCH : "p_search",
};

const DOM = {
  S_SUBMIT_SEARCH : 'p_search',
  S_DISPLAY_SEARCH : 'm_view',
};

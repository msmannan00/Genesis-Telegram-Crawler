const COMMANDS = {
  S_SUBMIT_VERIFICATION : 2,
  S_SUBMIT_TIMER : 3,
};

const KEY = {
  S_FEEDER_VERIFICATION : 'p_feeder_verification',
  S_COMMAND : 'p_command',
};

const DOM = {
  S_FEEDER_VERIFICATION : 'p_feeder_url_verification',
  S_FEEDER_TIMER : 'p_feeder_timer',
};
